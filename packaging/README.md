# Helix packaging

Phase 8 adds an explicitly unsigned AppX manifest. Run `npm run package:validate` to validate metadata. MSIX identity, certificate, release channel, and clean-machine installation procedure must be supplied before signing or publishing.
